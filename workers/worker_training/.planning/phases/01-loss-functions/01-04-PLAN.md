# Plan 4: Verify All Loss Tests Pass

## Objective
Run the full `tests/test_losses.py` suite and confirm every test is green; fix any remaining failures.

## Context
Plans 1-3 implement the three loss classes. This plan runs the tests, reads the output, and iterates on `src/training/losses.py` until all tests pass. The `TestFreqDomainLoss` tests in `tests/test_losses.py` import from `src.training.train_stage2` (Phase 4 scope) — they are expected to fail with `ModuleNotFoundError` and should be excluded from this phase's scope.

Only these test classes are in scope for Phase 1:
- `TestContrastiveLoss` (8 tests)
- `TestCrossModalAlignmentLoss` (5 tests)
- `TestSubjectAdversarialLoss` (4 tests)

Total: 17 tests must pass.

## Tasks

### Task 1: Run the loss tests and confirm results
**Action:** Execute pytest scoped to the three loss test classes and capture the output.
**File:** `src/training/losses.py` (fix if any tests fail)
**Details:**
Run the targeted test command:
```bash
pytest tests/test_losses.py::TestContrastiveLoss tests/test_losses.py::TestCrossModalAlignmentLoss tests/test_losses.py::TestSubjectAdversarialLoss -v
```
Read the output carefully. For each failure:
1. Identify which assertion failed and why
2. Fix the specific issue in `src/training/losses.py`
3. Re-run until all 17 tests pass

Common failure modes to watch for:
- `log_temp` buffer vs parameter: if `learnable_temp=False` uses `register_buffer`, confirm `len(list(loss_fn.parameters())) == 0`
- Temperature clamping: `torch.clamp(self.log_temp, ...)` — if `log_temp` is a buffer it's a tensor, if it's a float the clamp won't work. Keep it as a tensor in both cases.
- Zero-mask `requires_grad`: must return `torch.tensor(0.0, requires_grad=True)`, not `emb_a.new_zeros(1).squeeze()` (which may not have grad if `emb_a` doesn't)
- Alpha=0 gradient: `GradientReversalFunction.backward` returns `-ctx.alpha * grad_output` — with alpha=0.0 this is `0.0 * grad`, which is a zero tensor. Verify the test passes.

### Task 2: Run the full test file and report outcome
**Action:** Run the complete test file to confirm the 17 in-scope tests pass and the 3 out-of-scope `TestFreqDomainLoss` tests fail only with `ModuleNotFoundError` (not due to any losses code).
**File:** (read-only verification, no file changes expected)
**Details:**
```bash
pytest tests/test_losses.py -v 2>&1
```
Expected outcome:
- 17 tests PASSED (all loss tests)
- 3 tests ERROR/FAILED with `ModuleNotFoundError: No module named 'src.training.train_stage2'` — acceptable, Phase 4 scope

If any of the 17 loss tests fail after Task 1 fixes, return to Task 1 and iterate.

## Verification
- [ ] `pytest tests/test_losses.py::TestContrastiveLoss tests/test_losses.py::TestCrossModalAlignmentLoss tests/test_losses.py::TestSubjectAdversarialLoss -v` exits 0 with 17 passed
- [ ] No test in those 3 classes shows FAILED or ERROR

## Files Modified
- `src/training/losses.py` — any remaining fixes identified during test runs
